import BooksList from './BooksList';
import Navbar from './NavBar';

const Home = () => {
    return (
        <div>
            <Navbar />
            <BooksList />
        </div>
    );
};

export default Home;
